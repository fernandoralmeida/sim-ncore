namespace Sim.UI.Web.Functions
{
    public static class Extensions
    {
        public static string Contador(int numerador, int multiplicador)
        {
            return string.Format("{0}", numerador + multiplicador);
        }
    }
}
